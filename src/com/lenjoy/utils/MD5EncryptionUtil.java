package com.lenjoy.utils;

//自定义加密算法
public class MD5EncryptionUtil {
    //不加盐的加密算法
    //返回加密后的字符串
    public static String encryption(String value){
        String encryption = MD5EncryptionUtil.encryption(value);
        return encryption;
    }
}
